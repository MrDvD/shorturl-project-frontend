import { IgnoreDisabledValidators } from './IgnoreDisabledValidators';

describe('IgnoreDisabledValidators', () => {
  it('should create an instance', () => {
    const directive = new IgnoreDisabledValidators();
    expect(directive).toBeTruthy();
  });
});
