import { TakeValidators } from './TakeValidators';

describe('TakeValidators', () => {
  it('should create an instance', () => {
    const directive = new TakeValidators();
    expect(directive).toBeTruthy();
  });
});
